# VulTrial - Code Vulnerability Analyzer (Open Source)
# Python 3.10+ required

# Core dependencies for open-source models
torch>=2.0.0                # PyTorch for model inference
transformers>=4.35.0        # Hugging Face transformers
accelerate>=0.24.0          # For efficient model loading
bitsandbytes>=0.41.0        # For 8-bit and 4-bit quantization

# Optional: For faster inference
# optimum                   # Hugging Face optimization tools
# flash-attn                # Flash Attention for faster inference (requires CUDA)
